import { Routes } from '@angular/router';
import {DatePickersComponent} from "./date-pickers/date-pickers.component";

export const routes: Routes = [
  { path: 'pickers', component: DatePickersComponent }
];

